import { ThemeProvider } from "@/components/theme-provider"
import { cn } from "@/lib/utils"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import type React from "react"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Olofinsawe Ayomide - Technical Writer & Software Engineer",
  description:
    "Professional writing portfolio showcasing technical documentation, backend systems, DevOps, and cloud infrastructure writing from a software engineer's perspective.",
  openGraph: {
    title: "Olofinsawe Ayomide - Technical Writer & Software Engineer",
    description:
      "Technical writing and software engineering expertise in DevOps, backend systems, and cloud infrastructure.",
    type: "website",
  },
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={cn("min-h-screen bg-background font-sans antialiased", inter.className)}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
